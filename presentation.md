# 演讲稿：让 TChecker 时间自动机走进可视化时代

# 演讲稿：让 TChecker 时间自动机走进可视化时代

**各位老师、同学，大家好。我是本次分享的讲者，今天想和大家介绍我在 TChecker 上构建的图形化界面，让原本只存在于命令行和论文里的时间自动机验证流程，更贴近工程师和课程教学的真实场景。**

**我们先从背景说起。按照 Alur 和 Dill 的经典定义，一个时间自动机可以形式化为六元组 \$A=(L, l\_0, C, \\Sigma, E, \\text{Inv})\$：有限的位置集合、初始位置、时钟集合、事件字母表、带有时钟约束和重置的边，以及每个位置上的不变式。正是这些时钟约束，使得我们可以捕捉实时系统中“多久之前”“多久之后”这样的语义。但也因为引入了连续时间，模型的状态空间不再是简单的离散点，而是一簇簇由差分约束矩阵（DBM）描述的区域，这就需要像 TChecker 这样的专业验证器来进行符号状态探索。**

**TChecker 项目本身提供了 **`tck-reach`、`tck-simulate` 等命令工具，能够做基于区域图和 DBM 的广度优先搜索、死锁检测、轨迹生成等分析。不过在课程教学或企业评审里，直接操作命令行既不直观，也难以快速定位模型问题。这正是我开发 TChecker GUI 的初衷：把严谨的形式化内核保留下来，同时提供一个低门槛、可视化的入口。

**在建模层面，这个 GUI 以 Cytoscape.js 为核心，提供拖拽式的图形编辑器。我们可以在 Declarations 面板中声明全局时钟、整型变量和同步事件，随后在每个进程的画布里创建位置、设置初始态、写入不变式与标签，并为边编辑守卫、重置集和同步方向。底层的 **`tck-generator.js` 会把这些操作同步转换成合法的 `.tck` 文本，确保仍然与 TChecker 的语法兼容

**当模型初步成形后，可以直接进入交互式模拟。我们与 **`tck-simulate` 进行了流式集成：GUI 启动后会保持一个后台会话，按需把当前模型写入临时文件并推送给 TChecker。界面上会实时显示当前位置、可使能的边以及对应的时钟约束，用户一步步触发离散跃迁或演化时间。每一次响应其实都来自 TChecker 生成的 DOT 格式探索图，`simulation-manager.js` 会解析其中的 DBM，转写成“当前时钟对 0 的上界是多少”“两个时钟的差值是否受限”等信息。这样一来，学生在动手调度多个进程时，可以清晰看到差分约束是怎样演化的，也能快速发现被不变式卡住的瓶颈。

**在形式化验证阶段，界面提供了一个 Property 面板，对 **`tck-reach` 的常用能力做了封装：

1. **可达性检查** —— 指定目标标签，GUI 会用 `covreach` 算法跑广度优先搜索，并在成功时返回一条具体的执行路径作为证据。
2. **安全性检查** —— 如果某个“Bad”标签被推到可达，则立即给出反例轨迹，否则即证明系统安全。
3. **互斥性验证** —— 选择两个或多个标签，判断是否存在同时满足的状态，用来验证诸如 Fischer 算法的临界区互斥。
4. **死锁自由** —— 在没有额外标签的情况下，检测全局状态空间中是否存在不可推进的节点，一旦发现则给出导致死锁的调度。
5. **逻辑公式模式** —— 还支持输入标签的布尔公式，选择“存在”或“禁止”语义，GUI 会拆分成多次调用并聚合结果。

`verification-manager.js` 会根据不同的性质请求符号或具体证书；我们把 DOT 输出交给 `dot-parser.js`，再用 ReactFlow 绘制探索图，红色路径代表反例，蓝色路径代表证明。整套流程保持了 TChecker 的严谨性，却让验证人员不必反复切换终端和文本编辑器。

**我们在右侧还展示基本的统计量，例如状态数、边数、TChecker 报告的探索复杂度，帮助团队评估模型规模与验证开销。**

**如果把这些功能串联成一个演示，我通常会选 Fisher 互斥这个经典案例。先用图形编辑器画出两个相同的进程，声明全局时钟 **`x` 和共享变量 `turn`，在“等待”“尝试”“临界区”之间配置守卫。模拟时，我们可以看到时钟保持在 `[0,2]` 的不变式约束里张缩，计时器超过阈值就自动触发超时回退。接着切换到 Property 面板，选择两个进程的“critical”标签做互斥验证。GUI 会调用 `tck-reach`，几秒钟内返回“Mutual exclusion holds”并给出覆盖的状态数。现场的同学不仅看到结果，还能点开路径查看每次动作和时钟取值，这种反馈远比命令行上的“reachable false”来得直观。

**TODO：这里再加上fischer-async例子的演示**

**最后做个小结：时间自动机提供了精确刻画实时行为的理论支点，TChecker 则给了我们坚实的验证内核。而我制作的 TChecker GUI，把两者通过现代化的 Electron + React 技术组合在一起，在不牺牲形式化严谨性的前提下，让建模、模拟、验证三件事都变得可视、易懂、可分享。也期待大家亲自体验，并提出更多宝贵意见。谢谢大家！**

各位老师、同学，大家好。我是本次分享的讲者，今天想和大家介绍我在 TChecker 上构建的图形化界面，让原本只存在于命令行和论文里的时间自动机验证流程，更贴近工程师和课程教学的真实场景。

我们先从背景说起。按照 Alur 和 Dill 的经典定义，一个时间自动机可以形式化为六元组 $A=(L, l_0, C, \Sigma, E, \text{Inv})$：有限的位置集合、初始位置、时钟集合、事件字母表、带有时钟约束和重置的边，以及每个位置上的不变式。正是这些时钟约束，使得我们可以捕捉实时系统中“多久之前”“多久之后”这样的语义。但也因为引入了连续时间，模型的状态空间不再是简单的离散点，而是一簇簇由差分约束矩阵（DBM）描述的区域，这就需要像 TChecker 这样的专业验证器来进行符号状态探索。

TChecker 项目本身提供了 `tck-reach`、`tck-simulate` 等命令工具，能够做基于区域图和 DBM 的广度优先搜索、死锁检测、轨迹生成等分析。不过在课程教学或企业评审里，直接操作命令行既不直观，也难以快速定位模型问题。这正是我开发 TChecker GUI 的初衷：把严谨的形式化内核保留下来，同时提供一个低门槛、可视化的入口。

在建模层面，这个 GUI 以 Cytoscape.js 为核心，提供拖拽式的图形编辑器。我们可以在 Declarations 面板中声明全局时钟、整型变量和同步事件，随后在每个进程的画布里创建位置、设置初始态、写入不变式与标签，并为边编辑守卫、重置集和同步方向。底层的 `tck-generator.js` 会把这些操作同步转换成合法的 `.tck` 文本，确保仍然与 TChecker 的语法兼容，也支持从现有的 JSON 模型或 UPPAAL XTA 文件导入。

当模型初步成形后，可以直接进入交互式模拟。我们与 `tck-simulate` 进行了流式集成：GUI 启动后会保持一个后台会话，按需把当前模型写入临时文件并推送给 TChecker。界面上会实时显示当前位置、可使能的边以及对应的时钟约束，用户一步步触发离散跃迁或演化时间。每一次响应其实都来自 TChecker 生成的 DOT 格式探索图，`simulation-manager.js` 会解析其中的 DBM，转写成“当前时钟对 0 的上界是多少”“两个时钟的差值是否受限”等信息。这样一来，学生在动手调度多个进程时，可以清晰看到差分约束是怎样演化的，也能快速发现被不变式卡住的瓶颈。

在形式化验证阶段，界面提供了一个 Property 面板，对 `tck-reach` 的常用能力做了封装：

1. **可达性检查** —— 指定目标标签，GUI 会用 `covreach` 算法跑广度优先搜索，并在成功时返回一条具体的执行路径作为证据。
2. **安全性检查** —— 如果某个“Bad”标签被推到可达，则立即给出反例轨迹，否则即证明系统安全。
3. **互斥性验证** —— 选择两个或多个标签，判断是否存在同时满足的状态，用来验证诸如 Fischer 算法的临界区互斥。
4. **死锁自由** —— 在没有额外标签的情况下，检测全局状态空间中是否存在不可推进的节点，一旦发现则给出导致死锁的调度。
5. **逻辑公式模式** —— 还支持输入标签的布尔公式，选择“存在”或“禁止”语义，GUI 会拆分成多次调用并聚合结果。

`verification-manager.js` 会根据不同的性质请求符号或具体证书；我们把 DOT 输出交给 `dot-parser.js`，再用 ReactFlow 绘制探索图，红色路径代表反例，蓝色路径代表证明。整套流程保持了 TChecker 的严谨性，却让验证人员不必反复切换终端和文本编辑器。

值得一提的是，GUI 在模型管理上也做了配套：所有的建模数据都可以保存为 JSON，随时导出成 `.tck` 提供给 TChecker CLI 或 CI 流水线；内置了 Fisher 互斥、生产线等示例，方便教学快速上手。我们在右侧还展示基本的统计量，例如状态数、边数、TChecker 报告的探索复杂度，帮助团队评估模型规模与验证开销。

如果把这些功能串联成一个演示，我通常会选 Fisher 互斥这个经典案例。先用图形编辑器画出两个相同的进程，声明全局时钟 `x` 和共享变量 `turn`，在“等待”“尝试”“临界区”之间配置守卫。模拟时，我们可以看到时钟保持在 `[0,2]` 的不变式约束里张缩，计时器超过阈值就自动触发超时回退。接着切换到 Property 面板，选择两个进程的“critical”标签做互斥验证。GUI 会调用 `tck-reach`，几秒钟内返回“Mutual exclusion holds”并给出覆盖的状态数。现场的同学不仅看到结果，还能点开路径查看每次动作和时钟取值，这种反馈远比命令行上的“reachable false”来得直观。

未来的迭代方向，我们计划继续沿着 TChecker 的路线图演进：增加更多算法（例如 DBM 的 LU-关闭或超平面加速）、优化 DOT 图布局、让逻辑公式支持 TCTL 片段，甚至尝试把统计数据与教学平台打通。界面层面也会引入更多协同能力，比如注释、模板库，降低团队协作门槛。

最后做个小结：时间自动机提供了精确刻画实时行为的理论支点，TChecker 则给了我们坚实的验证内核。而我制作的 TChecker GUI，把两者通过现代化的 Electron + React 技术组合在一起，在不牺牲形式化严谨性的前提下，让建模、模拟、验证三件事都变得可视、易懂、可分享。也期待大家亲自体验，并提出更多宝贵意见。谢谢大家！
