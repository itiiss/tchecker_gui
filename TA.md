**Timed automaton**：在NFA的基础上，在转移上添加了**gurad和action**，在状态上添加了**invariant**

**时间流逝（Delay Transition）**：在任何给定的位置，系统都可以**让时间任意流逝**（即“延迟”），只要在此过程中**当前位置的不变式 (invariant)** 持续得到满足 。

**动作转换（Action Transition）**：当时间流逝到某个点，并且**转移上的守卫 (guard)** 条件得到满足时，自动机就可以选择执行一条满足条件的边，从当前位置**离散地转移**到下一个位置 。

**无限状态空间**：由于时钟是**实值变量**，它们的取值可以是任意非负实数，这导致了时间自动机的**具体状态空间是无限的** 。一个状态由**当前位置和当前时钟值**组成

**符号验证 (Symbolic Verification)**：为了在无限状态空间上进行验证，我们需要一种能够处理无限状态的方法。核心思想就是你所说的：通过将无限的“时钟赋值”集合表示为有限的、可操作的抽象形式

**区域 (Zones)**：这种抽象表示就是**区域 (zones)** 。一个区域是一个**时钟约束**，它代表了满足该约束的**所有时钟赋值的集合** 。我们可以将其理解为将连续的时间维度划分成离散的、等价的区域，这样就能用有限个符号状态来表示无限个具体状态。

**时间自动机语言**：时间迹 (t1,a1)(t2,a2)... 中，t是绝对时间，a是某条边上的action

**可判定性**：语言的包含性=不可判定；双模拟（互相包含，等价）和可达性 = 可判定

**转移是非强制的**

* **a 防止呆在某个状态idle（标记接受状态+要求必须无限访问接受状态）：****隐式地**对 `start` 和 `loop` 位置施加条件：`start` 必须在 `y` 值至多为 20 时离开，否则自动机将卡在 `start` 无法进入 `end`；同理，`loop` 必须在 `y` 值至多为 50 时离开才能进入 `end`
* **b则显示的添加invariant，呆在该状态时钟必须满足条件**

**2.1形式定义**

**在NFA的基础上，在转移上添加了****gurad和action**，在状态上添加了**invariant**

**2.2 时间转换 + 动作转换**

**时间流逝 + 选择满足边转移并执行action**

**2.3 包含性，双模拟和可达性**

**时间语言的包含性不可判定**

**双模拟和可达性可判定**

**3.1 Regions, Zones and Symbolic Semantics**

**region划分**

1. **整数部分相同或者都大于上限**
2. **小数部分为0的状态相同（同为0或者同不为0）**
3. **小数部分的顺序相同（x2.5-y3.5和x2.8-y3.8满足，但是x2.8-y3.5和x-2.5-y3.8不满足）**

**zone划分**

* **时钟与常量之间，以及时钟之间差值，与常量之间的不等式合取定义**
* **划分依据是自动机中所有时钟****约束条件的集合**

**Symbolic Semantics**

**在符号语义中，一个系统状态不再是 **`(位置, 具体时钟值)`，而是 `(位置, 区域)`

**对应的，其时间/动作转换变成：**

1. **符号延迟**：一个区域可以经过时间流逝，生成一个新的区域
2. **符号动作**：一个区域可以通过执行某个边上的动作，重置时钟，并移动到另一个位置，从而生成一个新的区域

**健全性与完备性：通过symbolic semantic建立的模型和真实系统的行为可以完全对应，是真实系统的抽象**

**3.2 非差分约束的Zone归一化**

**x的时钟每次会重置到0，但y的时钟可以无限增长，导致zone的数量可以是无限个**

**需要为那些可能无限增长的时钟，人为地找到一个最合理的“有效”上限，通常是****最大时钟常量**（图4的`y>=20`，对于y的上限则是20）

**通过这种方式，所有超出 20 的 **`y` 值，在归一化后都被抽象到有限的区域表示中

**K归一化**只能对无差分约束的时间自动机生效；因为人为设置上限后，无法捕捉到时钟间的相对值了，然后时钟的相对值是差分约束正确的关键（K归一化对于超过上限之后，就不再关心具体值了）

* **无差分约束：比如单个时钟的限制 **`x < 5, y < 3` ，找坐标图上是平行于xy轴的直线
* **差分约束：时钟和时钟间的约束，比如**`x - y < 2 => y < x+2`; 在坐标图上，是斜线 `y = x+2` 下面的区域

**3.3 含差分约束的Zone归一化**

1. **识别所有差分约束 g 和所有区域 D**
2. **对于每个区域D，检查D是否完全满足g或者完全不满足g**
3. **如果D是部分满足g，则将区域划分成完全满足g和完全不满足g的两部分**
4. **递归进行，知道没有区域部分满足约束g**

**3.4 Symbolic Reachability Analysis (符号可达性分析)**

* **安全性：如果不安全状态是不可达的，则整个系统是安全的 （3.4的重点）**
* **活性：某个好的事情一定发生（确保没有死循环无法发生）**

1. **将状态用 location + zone表示 （location表示自动机的状态，zone表示时间约束的时间段）**
2. **从初始状态开始，进行BFS**
3. **将从当前状态可访问的状态加入wait队列，访问过的状态加入visited队列**
   1. **当访问状态包含不安全状态时，算法返回不安全终止**
   2. **当wait队列为空时，算法返回安全终止**

**4.1 DBM 基础**

**Difference Bound Matrices，是一种高效表示和操作区域（zones）的核心数据结构**

* **时间自动机中定义了 **`n` 个定时器（时钟），就会决定 DBM 矩阵的大小为 `n+1`
* **元素 Dij**：存储约束 时钟 `j` 减去 时钟 `i` 的上界，比如 `(10，<), (-1, <), inf`，分别表示两个时钟的差值小于10，小于-1，小于无穷
  * **等效于约束表达式 **`xj - xi < 10` 这样的差分约束

**支持三类操作，4.2 属性检查 + 转化，和 4.3 zone归一化**

* **4.2 属性检查**
  * `consistent(D)`：是否存在一组具体的时钟值，能够同时满足 DBM 中所有的约束；对于规范化的 DBM，只需要看矩阵对角线上的元素是否全是整数
    * **约束存在矛盾，对应图论中存在负环 negative cycle**
    * **规范的过程其实是Floyd-Warshall算法，计算****所有节点对之间的最短路径**
    * **如果存在负环，那么存在某个元素 i 到 i 的 cost 为 负数**
  * `relation(D, D')`：判断**两个 DBM 所代表的区域之间是否存在包含关系**；只需要检查 `D` 中的每一个元素都大于 `D'` 中对应位置的元素
  * `satisfied(D, xi-xj<=m)`：再加上一个约束后，判断是否 `consistent`
* **4.2 转换**
  * `up(D)`：时间任意流逝后可以到达的状态集合，对应自动机的延迟转换delay transition的语义
  * `down(D)`：时间任何回溯可以到达的状态集合，获取上一步的所有可能状态
  * `and(D, g)`：将一个**守卫约束 g** 合取（AND）到 DBM D 上，对应自动机的动作转换的guard check
  * `free(D, x)`：移除timer x的所有约束，使其值可以取**任何正数**；对应时钟重置后，清除重置前的旧约束
  * `reset(D, x:=m)`：将时钟 x **重置**为值 m ，设置两个位置的元素 `x - 0 < m` 和 `x - 0 < -m` 来实现；对应自动机的动作转换的时钟重置
* **4.3 zone归一化**
  * `norm_k(D)`：对应3.2的非差分约束的归一化
  * `norm_k,G(D)`：对应3.3的差分约束的归一化

**4.4 zone的储存方式 Zones in Memory**

* **存储矩阵元素：1bit存储 **`< /  <=` , 其他 bit 存储上限的数字
* **降维度：将 n x n矩阵 按行存储为一维，**`loc(x, y) = x * (n+1) + y`
* **稀疏矩阵spare：DBM 在规范形式下，仍然可能包含****冗余约束**了；只存储表示区域所需的**最小约束集合**，并使用**稀疏表示**

**5.1 Upaall 建模**

* **核心**

  * **由多个时间自动机 A1,...,An ****并行组合**而成的系统
  * **使用 ****CCS 的并行组合操作符**来将这些自动机（称为**进程**）组合成一个单一的系统
* **通信**

  * **同步：使用****输入动作 (`a?`)** 和**输出动作 (`a!`)**
  * **异步：通过****共享整数变量**实现
* **语义**

  * **时间自动机网络的语义仍然是一个****转换系统** 。一个网络的状态是一个**tuple `(l, u)`**，其中 `l` 是一个**位置向量**（每个进程对应一个位置），`u` 是一个时钟赋值
  * **延迟转换：延迟转换规则与单个自动机相似**
  * **离散转换： 一个进程独立进行转换  or  两个进程在同一个通道上同步进行转换 **
* **urgent：不消耗时间**
* **commit：不能被中断 + 不消耗时间**

**5.2 Uppaal Verifying**

**Timed Computation Tree Logic：****形式化描述**时间自动机系统属性的**逻辑语言**

**TCTL 的语法和语义是从标准的****计算树逻辑 (CTL)** 扩展而来的，增加了对时钟和时间间隔的**时间量化**。

**TCTL 表达式由以下部分构成：**

1. **命题（Propositions）**：
   1. **位置（Locations）：**`l`，表示系统位于位置 `l`。
   2. **时钟约束（Clock constraints）：**`x <= n`, `x-y <= n` 等。
2. **布尔连接词（Boolean connectives）**：`¬`（非）、`∧`（与）、`∨`（或）。
3. **路径量词（Path quantifiers）**：
   * `A` (All )：表示“对于所有可能的路径”。
   * `E` (Exist)：表示“存在一条可能的路径”。
4. **时态模态（Temporal modalities）**：
   * `[]` (Always)：表示“在所有状态下”。
   * `<>` (Eventually)：表示“最终会到达某个状态”。
   * `U` (Until)：表示“一直到某个状态”。
   * `X` (Next)：表示“下一个状态”。

**TCTL 表达式用于描述两类属性：**

* **活性（Liveness）：**
  * **示例：**`A<>l`，表示“在**所有**可能的执行路径上，**最终**都会到达位置 `l`”。
  * **这是一个****不可达性**检查问题。
* **安全性（Safety）：**
  * **示例：**`A[](l => x<5)`，表示“在**所有**可能的执行路径上，如果系统位于位置 `l`，则**时钟 x 的值总是小于 5**”。
  * **这是一个****可达性**检查问题。

**TCTL 引入了时间量词，使得我们可以指定时间间隔。**

* **`[]_I`**：表示“**在时间间隔 I 内的所有**状态下”。
* **`<>_I`**：表示“**在时间间隔 I 内的某个**状态下”。
* **`U_I`**：表示“**一直到**在时间间隔 I 内到达某个状态”。

**例如：**`A<>_<=5l`，表示“对于所有可能的路径，**在未来的 5 个时间单位内**，系统**最终**会到达位置 `l`”。

---

`cd build`

`make tck-matrix`

```


./src/tck-matrix -i ../example_model.tck          # Initial zone matrix
./src/tck-matrix -d ../example_model.tck           # Detailed output (constraints+matrix)  
./src/tck-matrix -s 5 ../simple_fischer.tck       # Simulation 5 steps showing zone evolution
./src/tck-matrix -o output.txt ../example_model.tck    # Output to file
```

---

** Enabled来源**

* **renderer 在 initializeSimulator 里把当前模型转成后端需要的 JSON，然后通过 IPC 调用 initialize-simulator（src/renderer/src/store/editorStore.js:571）。**
* **主进程收到请求后生成临时 .tck 文件，随后调用 tck-simulate temp.tck -t -r 3 -o init\_output.dot（src/main/utils/simulation-manager.js:493 与 :405），也就是把 TCK 作为第一个参数传给 tck-** **simulate。**
* **tck-simulate 不把结果直接写回进程，而是把随机模拟出来的执行轨迹输出到 init\_output.dot，后端再用 parseDot 读成 JSON，并从初始节点的出边当作 enabled transitions 返回（src/main/utils/** **simulation-manager.js:515）。**

**  问题原因**

* **-r N 选项触发的是“随机模拟 N 步”，配合 -t 只会得到这条随机路径上的边，而不是初始状态下所有可选的出边。针对 fisher.tck 手动运行 src/main/build/src/tck-simulate fisher.tck -t -r 3 -o** **init.dot 可以看到 DOT 文件里初态 0 只有一条出边 P8@tau，因此前端最终也只展示一个 enabled transition。**
* **前端在 parseBackendTransitions 里只是把后端返回的这些边转成可展示结构（src/renderer/src/store/editorStore.js:1026 起），不会补齐缺失的分支，因此 enabled transitions 看上去被“削减”了。**

**  建议**

* **如果想要拿到初态下全部可启用的转换，需要改用能列举所有后继的指令（例如去掉 -r，或者改为调用 reachability/graph 枚举命令），或在 DOT 解析前先让后端把所有同源节点的边合并补齐。**

**我想要的效果是，进入simulator后，展示可以选择的所有转换，用户可以选择其中一个转换，然后沿着这个方向一直探索，tck-simulate有一步一步调试展示当前最新结果的调用方法吗**
